server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/EnzoFerreira4152/microservices-practice.git
          default-label: main
          search-paths: config-files